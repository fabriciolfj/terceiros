apply plugin: 'java-library'

ext.moduleName = 'coffeeandit.example.db.hazelcast'

dependencies {
	implementation(
		project(':domain'),
		project(':usecase'),
		'com.hazelcast:hazelcast:3.9.3',
	)
}
